# Cleanup Notes

2026-06-23 first pass:

- Added ignore rules for LaTeX build products, temporary remote scripts, installers, archives, local agent state, Python caches, and large generated artifacts.
- Current manuscript root is still unresolved.
- Current code root is still unresolved between `project/` and `project-new/`.
- Remote run source: `/root/ltl_repro_work_20260618`.
