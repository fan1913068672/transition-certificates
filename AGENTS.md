# Codex Rules

This repository is the persistence-certificate paper workspace.

- Confirm the active manuscript root before reorganizing: root `main.tex`, `latest-paper/`, or `els-cas-templates/main.tex`.
- Treat `project-new/` as the current reproducibility candidate until confirmed otherwise.
- Do not commit local installers, micromamba payloads, dReal packages, remote scratch scripts, LaTeX build output, or raw result pools.
- Remote work under `/root/ltl_repro_work_20260618` should be summarized in manifests before any local cleanup.
- Do not delete uncommitted manuscript or experiment changes; quarantine or document first.
